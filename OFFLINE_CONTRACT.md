# Offline contract — Recipes

| Capability | Offline |
| --- | --- |
| Launch | Yes if cached |
| Cached recipe browse | Yes where the last catalogue is cached |
| Recipe/card/hero assets | Runtime-cached after first fetch |
| Saved recipes | Yes if stored locally |
| Shopping handoff | Queued if offline, sent when Get List/suite is reachable |
| Recipe Studio generate/publish | Requires internet (private Railway service) |
| Requires internet | Fresh catalogue, first load, Studio generation, live shopping sync |
